export enum AdminPermissions {
  READ = "read",
  CREATE = "create",
  UPDATE = "update",
  DELETE = "delete",
}

export enum UserRole {
  ADMIN = "admin",
  USER = "user",
}

export enum RoomType {
  SINGLE = "single",
  DOUBLE = "double",
}
